# Source-backed shape taxonomy

Vehicle Shape Signals use a source-backed manual taxonomy rather than keyword rules or unreferenced model knowledge. This is slower to build, but the dashboard is public and safety-facing, so each reviewed `generic_make_model` row needs a source URL to make the SUV/crossover versus other passenger car classification auditable.
